<% content_for :title, t("app.title") %>

<%= render "dashboard/auth_overlay" %>

<div class="dashboard">
  <%= render "dashboard/header" %>

  <% if @briefing %>
    <!-- Row 1: Glanceable widgets (equal height) -->
    <div class="dashboard__widgets-row">
      <%= render "dashboard/date_widget", briefing: @briefing %>
      <%= render "dashboard/weather_widget", briefing: @briefing %>
      <%= render "dashboard/daily_goals_widget", briefing: @briefing %>
    </div>

    <!-- Action Items -->
    <%= render "dashboard/section", title: t("dashboard.sections.action_items"), icon: "list-checks" do %>
      <%= render "dashboard/action_items", briefing: @briefing %>
    <% end %>

    <!-- Long Term Goals -->
    <%= render "dashboard/section", title: t("dashboard.sections.long_term_goals"), icon: "target" do %>
      <%= render "dashboard/long_term_goals", briefing: @briefing %>
    <% end %>

    <!-- Agent Activity -->
    <%= render "dashboard/section", title: t("dashboard.sections.agent_activity"), icon: "bot" do %>
      <%= render "dashboard/agent_activity", briefing: @briefing %>
    <% end %>

    <%= render "dashboard/footer", briefing: @briefing %>
  <% else %>
    <div class="dashboard__empty">
      <i data-lucide="sun"></i>
      <p><%= t("dashboard.no_briefing") %></p>
    </div>
  <% end %>
</div>
